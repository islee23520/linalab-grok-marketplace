---
description: Show CLIProxyAPI connectivity and Grok managed-config status
argument-hint: "[--json]"
---

Run the provider status check and report its result:

```bash
node "${GROK_PLUGIN_ROOT}/dist/cli.mjs" status $ARGUMENTS
```
